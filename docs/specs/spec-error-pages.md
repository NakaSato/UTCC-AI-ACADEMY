---
id: SPEC-0045
type: spec
title: Error pages for every failed request, rendered and flat
status: accepted
owners: ["@tech-lead", "@platform-owner", "@product-owner"]
created: 2026-08-09
updated: 2026-08-12
review_by: 2026-11-07
supersedes: []
superseded_by: []
depends_on: [ADR-0045, ADR-0020, SPEC-0020]
implemented_by:
  - app/models/http_error.rb
  - app/controllers/errors_controller.rb
  - app/controllers/concerns/localization.rb
  - app/views/layouts/error.html.erb
  - app/views/errors/show.html.erb
  - lib/error_pages.rb
  - lib/templates/error_page.html.erb
  - lib/tasks/error_pages.rake
  - config/application.rb
  - config/routes.rb
touches:
  - app/controllers
  - app/models
  - app/views
  - lib
  - config/routes.rb
  - config/application.rb
  - config/locales/en.yml
  - config/locales/th.yml
  - public
enforced_by:
  - test/controllers/errors_test.rb
  - test/models/http_error_test.rb
  - test/lib/error_pages_test.rb
  - test/release/deployment_configuration_test.rb
agent_writable: true
requires_skills: [SKILL-SPEC-001, SKILL-SPEC-002, SKILL-PROD-001]
min_reviewer_skills: [SKILL-SPEC-002]
---

# Error Pages for Every Failed Request, Rendered and Flat

> [Specifications](README.md) ·
> [Rendered error pages decision](../decisions/adr-0045-rendered-error-pages.md) ·
> [Critical-failure observability specification](spec-m9-critical-failure-observability.md)

## Problem

A failure was the only part of the product that was not the product. Rails'
generated files answered 400, 404, 406, 422 and 500 in English, unstyled, with
no link back into the academy — on an app whose default language is Thai. The
statuses a visitor most often meets during a deploy, 502 and 503 and 504, had no
page at all. And a 5xx never showed the request id that SPEC-0020 stamps on the
matching alert, so the one number that would let support find the failure was
the one number the person reporting it could not see.

The page also has to survive what it reports. The application layout reads a
learner's gems, streak and notifications on every screen; rendering a 500
through it means a database outage raises twice and the visitor gets the flat
file anyway.

## Invariants

1. `config.exceptions_app` is the router. A failed request is re-dispatched with
   its path rewritten to its status code, on its original verb.
2. `ErrorsController` inherits `ActionController::Base` and includes only
   `Localization`. No authentication, no authorization, no observability
   callback, no browser gate.
3. Rendering an error page performs no database query, and the error layout
   renders no session-dependent chrome.
4. `HttpError::NAMED` — 400, 403, 404, 406, 422, 429, 500, 502, 503, 504 — have
   copy of their own in both locale files. Any other status in 400..599 renders
   using `error_pages.client` or `error_pages.server`, chosen by whether the
   code is below 500.
5. A status outside 400..599 is not an error page: the route constraint is
   `[45]\d\d`, so `/700` is a routing error like any other unknown path.
6. The response carries the status it describes, `noindex, nofollow`, and
   no-cache.
7. A 5xx page prints `request.request_id`; a 4xx page does not.
8. A failed request whose format is not HTML gets the status and an empty body,
   including when the `Accept` header is what was malformed.
9. `HttpError::STATIC_PAGES` names the flat files in `public/`: 400, 404, 422,
   500, 502, 503, 504, and `406-unsupported-browser.html`, whose name is fixed
   by `allow_browser`.
9a. `docs/maintenance.html` is a ninth generated page: the 503 as Render's
    maintenance mode serves it, from the documentation site rather than from the
    app. Every URL in it is absolute and it requests no asset, because it is
    served while the app is down. `render.yaml` carries
    `maintenanceMode.enabled: false` and a `uri` naming the path the
    documentation host serves that page at — not the file name, which is not the
    same string whenever the host rewrites URLs.
9b. `bin/rails error_pages:published` fetches that `uri` and fails unless it
    answers 200 with the exact page this repository generates. A redirect, a
    404, and a live-but-older copy each fail with their own reason.
10. The flat files are generated by `bin/rails error_pages:build` from the same
    `error_pages` copy, print Thai and English together, and reference no
    stylesheet, script or asset the app would have to be running to serve.
11. What is committed in `public/` matches what the generator produces.
12. The live pages are reachable at `/errors/:code` as well as at `/:code`,
    because the flat files shadow the bare paths whenever the static file
    server is on.
13. `/errors` is disallowed in `robots.txt`.

## Acceptance Criteria

- Each of the ten named statuses renders at `/errors/<code>` with that status
  and its own title.
- `/errors/409` renders with status 409 and the client-family title.
- `/errors/700` is a routing error, not a rendered page.
- `/errors/404?lang=en` renders the English title; without the parameter a
  Thai-preferring visitor gets Thai.
- `/errors/404` carries `noindex, nofollow` and a no-cache `Cache-Control`.
- `/errors/500` shows the request id of that request; `/errors/404` shows no
  reference line at all.
- `/errors/500` requested as JSON returns 500 with an empty body.
- `POST /422`, dispatched as `exceptions_app` dispatches it, answers 422.
- `/errors/500` renders with no database query and no session.
- A request for a route that does not exist renders the branded 404 through
  `exceptions_app`, not Rails' generated file.
- Every file named in `HttpError::STATIC_PAGES` exists in `public/` and matches
  the generator's output.
- `public/503.html` contains both the Thai and the English title and no `<link
  rel="stylesheet">` or `<script>`.

## Verification

- `test/controllers/errors_test.rb` covers the rendered pages: every named
  status, the family fallback, the constraint, the language, the robots and
  cache headers, the request id on a 5xx and its absence on a 4xx, the non-HTML
  response, the verb, the absence of queries, and the `exceptions_app` dispatch.
- `test/models/http_error_test.rb` covers the copy fallback in both directions,
  the client/server split, the range clamp, and that every named status has
  `name`, `title` and `body` in both locales.
- `test/lib/error_pages_test.rb` covers the freshness of `public/`, the presence
  of each file, the browser file's fixed name, and that a flat page is
  bilingual and self-contained.
- `test/controllers/crawlers_test.rb` continues to cover `robots.txt`.
- `bin/verify` runs the whole gate.

## Amendment, 2026-08-11: 503 is wired to Render's maintenance mode

`render.yaml` gains `maintenanceMode` with `enabled: false` and a `uri` naming
`docs/maintenance.html`, published by the documentation site. Render answers a
maintained service with 503 and serves that page, so planned downtime now shows
the academy's own words in both languages instead of Render's default.

Three things worth keeping straight:

- **The page cannot live on the app.** Render requires a `uri` that is not the
  service in maintenance, which is the point — the service is what is
  unavailable. So it is generated a second time with absolute URLs and no icon,
  because a root-relative asset would resolve against the docs origin and an
  absolute one would ask the dead service for it.
- **The state belongs to the dashboard, not the repository.** `enabled` stays
  `false` in `render.yaml` and a test enforces that. Committing `true` couples
  an operational switch to a release and leaves the repository claiming
  maintenance long after it ended.
- **It publishes on Vercel, not GitHub Pages.** An earlier draft of this
  amendment said the documentation site had never published and the `uri` was
  inert. That was wrong: the site is live at `utcc-ai-academy.vercel.app`,
  built from this repository by `script/build-dashboard` on push to `main`, and
  has been throughout. The mistake was reading a failing
  `.github/workflows/pages.yml` — refused for an account billing lock — as the
  only publisher, when `vercel.json` sits in the repository root. The `uri`
  pointed at the `github.io` address, which does not resolve, and now points at
  the Vercel one.

## Amendment, 2026-08-12: the `uri` is a path on the host, not a file name

Moving the `uri` to the right host did not make it resolve. It still named
`/maintenance.html`, and `vercel.json` sets `cleanUrls: true`, so the
documentation site serves the page at `/maintenance` and answers the `.html`
form with a 308. Meanwhile the deploy that would have published the page at all
had been failing since 2026-08-09, so both forms returned 404 — three days in
which planned downtime would have shown Render's default page while the
repository, its tests, and its runbook all said otherwise.

Two things follow, and both are about the check rather than the URL:

- **The `uri` is derived from the publishing rule, not from the file name.**
  `test/release/deployment_configuration_test.rb` reads `cleanUrls` out of
  `vercel.json` and asserts the `uri` path is what the host will actually serve
  the generated page at. Flipping that setting now fails a test instead of
  silently costing a redirect hop.
- **Nothing local can tell you this page is up.** `error_pages:check` compares
  the repository against itself and passed throughout. The page lives on another
  origin, published by another pipeline, and the failure mode is a deploy that
  aborts and leaves the previous copy online looking current. So
  `bin/rails error_pages:published` fetches the `uri` and compares the body to
  `ErrorPages.hosted`. It is not in `bin/verify` — it asserts a fact about the
  outside world, and it is a precondition of RB-0006, which is the moment the
  answer matters.

## Out of scope

**502 and 504 remain the platform's.** When Render has no container to route to,
or the one it has is too slow, nothing in this repository is asked and its
blueprint exposes no hook to change that. `public/502.html` and
`public/504.html` are kept for a proxy that can be told otherwise — they are not
reachable through Render today, and RB-0006 says so plainly so nobody promises
a branded 502.

**503 is now wired** — see the amendment below — which is the half of this that
turned out to be reachable.

Rate limiting is also unchanged. The six controllers that throttle already
redirect with a flash rather than returning a bare 429, which is a better answer
for a form than any page would be; `error_pages.429` exists for a 429 that
arrives without one.
