# Async adapter only works within the same process, so for manually triggering cable updates from a console,
# and seeing results in the browser, you must do so from the web console (running inside the dev process),
# not a terminal started via bin/rails console! Add "console" to any action or any ERB template view
# to make the web console appear.
development:
  adapter: async

test:
  adapter: test

# No `connects_to`: solid_cable_messages lives in the primary database, so the
# 100ms poll runs on the pool the app already has instead of one of its own. See
# config/database.yml for the connection budget that decides this — and note that
# what keeps the poll cheap is message_retention below, not the isolation.
production:
  adapter: solid_cable
  polling_interval: 0.1.seconds
  message_retention: 1.day
