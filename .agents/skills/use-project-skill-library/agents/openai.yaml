interface:
  display_name: "Project Skill Library"
  short_description: "Route work through the project skill library"
  default_prompt: "Use $use-project-skill-library to select and apply the project skills relevant to this task."
policy:
  allow_implicit_invocation: true
